import React, { useState } from 'react';

const FriendSearch = () => {
	const searchSection = ['friends', 'people'];
	const [search, setSearch] = useState('friends');
	return <div>FriendSearch</div>;
};

export default FriendSearch;
